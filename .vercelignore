# Keep the serverless bundle small — none of this is needed to serve the app.
tests/
notebooks/
docs/
.github/
artifacts/
var/
*.ipynb
Dockerfile
docker-compose.yml
Makefile

# Build artefacts from a local `python -m build`.
build/
