# Runtime for the service (echo engine — no GPU or ML stack required).
# For Gemma 3n inference:  pip install -e '.[ml]'
# For fine-tuning:         pip install -e '.[train]'
# For spoken replies:      pip install -e '.[speech]'
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
numpy>=1.24
pillow>=10.0
